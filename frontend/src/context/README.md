# Context

React Context providers for global state management.

Contains context providers and consumers for app-wide state like authentication, theme, language, etc. Simpler alternative to Redux for managing shared state.
