# Styles

Global CSS files and style configurations.

Contains CSS that applies across the entire app (reset, variables, animations, etc.). Component-specific styles should be co-located with their components.
