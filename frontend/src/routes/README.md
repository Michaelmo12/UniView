# Routes

Routing configuration that maps URLs to page components.

Contains the main router setup and any route protection logic (PrivateRoute, public routes, etc.). This is where you define which URLs load which pages.
