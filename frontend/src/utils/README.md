# Utils

Utility functions and helper methods.

Pure JavaScript/TypeScript functions that don't depend on React (formatDate, validators, constants, etc.). If it doesn't use hooks or JSX, it belongs here.
