# Components

Reusable UI components that can be used across the entire application.

These are generic, framework-like components (Button, Input, Card, Modal, etc.) that are not tied to specific features or business logic.