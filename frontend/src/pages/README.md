# Pages

Page components that correspond to routes in the application.

Each file represents what the user sees at a specific URL. Pages compose together components and feature modules to create complete views.
