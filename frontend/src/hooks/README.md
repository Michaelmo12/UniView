# Hooks

Custom React hooks used globally across the application.

Contains reusable hooks that manage state or side effects (useFetch, useAuth, useLocalStorage, etc.). All files must start with 'use' and can only be called inside React components.
